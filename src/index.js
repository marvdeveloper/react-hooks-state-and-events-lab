import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./index.css"; // Optional: only if you want styling

ReactDOM.render(<App />, document.getElementById("root"));
